Here is what you will do. 

Before that here is what i want at the end: I want this entire application to be dockerised while having fastAPI endpoints which serve the model. The port for the docker container should be set via the docker compose file. 
1. Get to know the entire project structure
2. Create a venv and requirements.txt
3. Create fastapi endpoints for the application with a specific endpoint which takes the parameters required for the model for prediction and returns the predicted value. 
4. create model.py for the JSON contracts. 
5. Test the application( you can find sample data in the main.py)
6. write a dockerfile to build
7. write a docker compose file for deployment.

At every stage, write tests and check the working of application. 